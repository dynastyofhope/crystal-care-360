/* ============================================================================
   supabase-config.js — THE ONLY FILE YOU EDIT TO GO LIVE
   ----------------------------------------------------------------------------
   1. Create your project at https://supabase.com  (see SUPABASE-SETUP.md)
   2. Dashboard → Project Settings → API
   3. Copy "Project URL" and the "anon public" key into the two lines below
   4. Upload the site again. Done — the pages switch from demo to real accounts
      and a real database automatically.

   Leave both lines empty ('') and everything keeps working in demo mode —
   the pages behave exactly as they do now, with no internet needed.
   ========================================================================== */
window.SYL4LUV_CONFIG = {

  /* Dr. Syl4luv's live database — project xggonyabxjkzaxakvdpt (West EU · free tier) */
  supabaseUrl: 'https://xggonyabxjkzaxakvdpt.supabase.co',

  /* the publishable key (the new name for the "anon public" key).
     It is meant to be public — safe in this file and in the uploaded page.
     What protects the members is the row-level security in schema.sql.
     NEVER put the "service_role" key in a webpage — that one bypasses the rules. */
  supabaseAnonKey: 'sb_publishable_VMIs6zK32Ksz5B9-m0eh5w_v_NbPVEO'

};
